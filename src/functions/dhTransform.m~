function [dh] = dhTransform(d, a ,theta, alpha)
dh = [cos(theta) -sin(theta)*cos(alpha) sind(theta)*sind(alpha) a*cosd(theta);
      sin(theta) cos(theta)*cos(alpha) -cosd(theta)*sind(alpha) a*sind(theta);
      0 sin(alpha) cos(alpha) d;
      0 0 0 1];
dh = vpa(dh, 2);
return